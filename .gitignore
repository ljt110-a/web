# 本机真实配置：里面有数据库口令，绝不提交
src/config.local.php

# 运行期产物（日志 / 开发期的邮件 / 数据库备份 / 测试输出）
# 全部落在 var/ 下，都是一次性的、且可能包含用户数据，不该进版本库
var/

# 安装脚本生成的运行时文件
database/*.bak

# 编辑器 / 系统杂项
.vscode/
.idea/
Thumbs.db
.DS_Store
